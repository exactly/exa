appId: ${APP_ID ?? "app.exactly"}
---
- runFlow:
    file: copyAria.yaml
    env: { aria: "^(US)?\\$[\\s\\d,.\\xa0]+$" }
- evalScript: ${output.portfolio = Number(maestro.copiedText.replace(/\D/g, "")) / 100}
