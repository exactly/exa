syntax = "proto3";

package exa;

message Accounts {
  repeated string accounts = 1;
}

message AccountEvents {
  repeated PluginInstalled plugins = 1;

  message PluginInstalled {
    bytes plugin = 1;
    bytes account = 2;
    bytes manifest_hash = 3;
    uint64 log_ordinal = 4;
  }
}
