{
  "name": "@exactly/substreams",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release --quiet",
    "rustup": "rustup show",
    "test": "nx test substreams",
    "test:clippy": "cargo clippy --quiet -- --deny warnings",
    "test:fmt": "cargo fmt --check",
    "test:protolint": "protolint proto"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.66.1",
    "@exactly/plugin": "workspace:^1.1.0",
    "protolint": "^0.56.4"
  },
  "nx": {
    "projectType": "application",
    "targets": {
      "build": {
        "dependsOn": [
          "rustup",
          "^build"
        ],
        "inputs": [
          "{projectRoot}/.cargo/config.toml",
          "{projectRoot}/build.rs",
          "{projectRoot}/buf.gen.yaml",
          "{projectRoot}/Cargo.lock",
          "{projectRoot}/Cargo.toml",
          "{projectRoot}/proto/**",
          "{projectRoot}/rust-toolchain.toml",
          "{projectRoot}/rustfmt.toml",
          "{projectRoot}/src/**",
          "!{projectRoot}/src/contracts/**",
          "!{projectRoot}/src/proto/**",
          "{projectRoot}/substreams.yaml",
          {
            "dependentTasksOutputFiles": "contracts/out/build/ExaAccountFactory.sol/ExaAccountFactory.json"
          },
          {
            "dependentTasksOutputFiles": "contracts/out/build/UpgradeableModularAccount.sol/UpgradeableModularAccount.json"
          }
        ],
        "outputs": [
          "{projectRoot}/src/contracts",
          "{projectRoot}/src/proto",
          "{projectRoot}/target/wasm32-unknown-unknown/release/substreams.wasm"
        ]
      },
      "test": {
        "dependsOn": [
          "test:clippy",
          "test:fmt",
          "test:protolint"
        ],
        "executor": "nx:noop"
      },
      "test:clippy": {
        "dependsOn": [
          "build",
          "prepare",
          "^prepare",
          "^build",
          "rustup"
        ]
      },
      "test:fmt": {
        "dependsOn": [
          "test:clippy"
        ]
      }
    }
  }
}
