import api from ".";
import supervise from "../supervise";
import secret from "../utils/secret";

supervise(
  "api",
  Promise.all([
    secret("api-alchemy-webhooks-key"),
    secret("api-auth-secret"),
    secret("api-bridge-api-key"),
    secret("bridge-api-url"),
    secret("chat-identity-key"),
    secret("api-intercom-identity-key"),
    secret("api-manteca-api-key"),
    secret("manteca-api-url"),
    secret("api-panda-api-key"),
    secret("panda-api-url"),
    secret("api-pax-associate-id-key"),
    secret("api-pax-api-key"),
    secret("pax-api-url"),
    secret("api-persona-api-key"),
    secret("persona-api-url"),
    secret("api-postgres-url"),
    secret("redis-url"),
    secret("api-sardine-api-key"),
    secret("sardine-api-url"),
    secret("api-segment-write-key"),
    secret("api-wallet-extension-secret"),
    secret("api-whatsapp-access-token"),
    secret("whatsapp-phone-number-id"),
  ]).then(
    ([
      alchemyKey,
      authSecret,
      bridgeKey,
      bridgeUrl,
      chatKey,
      intercomKey,
      mantecaKey,
      mantecaUrl,
      pandaKey,
      pandaUrl,
      paxAssociateKey,
      paxKey,
      paxUrl,
      personaKey,
      personaUrl,
      postgresUrl,
      redisUrl,
      sardineKey,
      sardineUrl,
      segmentKey,
      walletExtensionSecret,
      whatsappToken,
      whatsappFrom,
    ]) =>
      api({
        alchemyKey,
        authSecret,
        bridgeKey,
        bridgeUrl,
        chatKey,
        intercomKey,
        mantecaKey,
        mantecaUrl,
        pandaKey,
        pandaUrl,
        paxAssociateKey,
        paxKey,
        paxUrl,
        personaKey,
        personaUrl,
        postgresUrl,
        redisUrl,
        sardineKey,
        sardineUrl,
        segmentKey,
        walletExtensionSecret,
        whatsappFrom,
        whatsappToken,
      }),
  ),
);
