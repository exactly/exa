name: substreams/sandbox
on:
  push:
    branches: [web]
concurrency: server-sandbox
jobs:
  deploy:
    uses: ./.github/workflows/substreams-deploy.yaml
    secrets: inherit
    with:
      environment: sandbox
