name: server/build
on:
  workflow_call:
    inputs:
      stack: { required: true, type: string }
      chain: { required: true, type: number }
      domain: { type: string }
      devtools: { default: false, type: boolean }
jobs:
  docker:
    runs-on: ubuntu-latest
    environment:
      name: package
      url: https://github.com/orgs/${{ github.repository_owner }}/packages/container/package/${{ github.event.repository.name }}-${{ inputs.stack }}
    permissions: { artifact-metadata: write, contents: read, packages: write, attestations: write, id-token: write }
    env:
      IMAGE: ghcr.io/${{ github.repository }}-${{ inputs.stack }}
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0, fetch-tags: true, persist-credentials: false }
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=sha,format=long
            type=ref,event=pr
            type=ref,event=branch
            type=match,pattern=@exactly\/server@(\d+\.\d+\.\d+.*),group=1
            type=match,pattern=@exactly\/server@(\d+\.\d+),group=1
            type=match,pattern=@exactly\/server@(\d+),group=1
      - uses: docker/login-action@v4
        with: { registry: ghcr.io, username: "${{ github.repository_owner }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/build-push-action@v7
        id: build
        with:
          push: true
          context: .
          file: server/Dockerfile
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          no-cache: true
          build-args: |
            CHAIN_ID=${{ inputs.chain }}
            APP_DOMAIN=${{ inputs.domain || format('{0}.exactly.app', inputs.stack) }}
            EXPO_PUBLIC_DEVTOOLS=${{ inputs.devtools }}
      - uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.IMAGE }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
